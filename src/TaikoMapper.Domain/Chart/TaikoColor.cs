namespace TaikoMapper.Domain.Chart;

/// <summary>
/// The two taiko note colors: <see cref="Don"/> is the red centre hit, <see cref="Kat"/> is
/// the blue rim hit.
/// </summary>
public enum TaikoColor
{
    /// <summary>Red centre hit ("don").</summary>
    Don,

    /// <summary>Blue rim hit ("kat").</summary>
    Kat,
}
